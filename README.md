# homebrew-tap

Homebrew tap for [Headroom](https://github.com/GentaAmeku/headroom) — a macOS menu bar app showing AI coding tool usage at a glance.

## Install

```sh
brew install --cask --no-quarantine GentaAmeku/tap/headroom
```

`--no-quarantine` is recommended because the app is not code-signed.
